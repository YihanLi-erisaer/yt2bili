# yt2bili 桌面端三期总实现方案

编写日期：2026-09-19  
适用项目：当前仓库 `yt2bili`，现有 Python 包版本 `0.1.0`  
文档性质：三期工程实施方案。2026-09-20 已按第一期实施桌面开发版本；当前实现、启动方法及已验证/待补验范围见 [第一期开发与验收记录](第一期开发与验收记录.md) 与 [桌面版说明](desktop/README.md)。下文保留原始三期规划，第二、三期尚未实施。

## 1. 目标与交付边界

在现有下载、翻译、校验和投稿能力上增加可视化桌面操作界面，视觉延续 [StarDazz 工作室官网](https://stardazz-com.vercel.app/)，分三期交付：

1. **第一期：可视化操作界面。** 完成可运行的桌面开发版本，用户可以在界面中完成配置、登录、批量任务、进度查看、素材预览、投稿、失败重试和修复。
2. **第二期：Windows 安装程序。** 将第一期应用及运行依赖打包，交付可下载安装的 `yt2bili-<version>-windows-x64-setup.exe`。
3. **第三期：macOS 应用与安装镜像。** 复用同一套界面和业务核心，完成平台适配、签名与公证，交付 Apple Silicon 和 Intel 两种架构的 `.dmg`。

三期在同一仓库、同一业务核心上逐步推进。第一期就确定跨平台路径和通信契约，第二、三期主要完成依赖分发与系统集成。

说明：Windows 的 `setup.exe` 是安装器，安装后启动实际应用程序；macOS 的 `.dmg` 是分发镜像，用户拖拽安装其中的 `yt2bili.app` 后运行。不能把命令行脚本改扩展名作为这两类交付。

最初方案交付仅包含文档；用户后续已授权第一期开发。本次新增桌面开发版本，不生成面向用户分发的安装包，不执行真实投稿。

## 2. 当前工程基线与改造依据

### 2.1 已有能力及复用位置

- `yt2bili/cli.py`：提供 `setup`、`login`、`renew`、`youtube-cookies`、`run`、`retry`、`repair`、`list` 命令。
- `yt2bili/pipeline.py`：串联处理流程；批量任务已有下载、校验、上传三个独立 FIFO 阶段，每阶段一个工作线程，各阶段可以同时运行。
- `yt2bili/youtube.py`：使用 yt-dlp 读取元数据、下载视频、处理 Cookie 和寻找 JavaScript 运行时。
- `yt2bili/media.py`：FFmpeg/ffprobe 检查、完整音视频校验、校验缓存、必要时的格式转换、封面处理。
- `yt2bili/translate.py`：DeepL 翻译与简介来源信息拼接。
- `yt2bili/bili_upload.py`：biliup 查找和下载、扫码登录、刷新登录态、串行上传及 BV 号解析。
- `yt2bili/db.py`：SQLite 任务存储，视频 ID 为主键，包含状态、原文、译文、文件路径、错误和 BV 号。
- `res/yt2bili_icon.jpg`、`res/yt2bili_darkIcon.jpg`：已有品牌图像，可作为界面及桌面图标加工来源；发布前核对清晰度、留白和透明背景。
- `tests/test_stage_queues.py`、`tests/test_pipeline_concurrency.py`、`tests/test_media_safety.py`：已有队列、并发和媒体安全回归测试。

### 2.2 必须提前解决的问题

1. **路径依赖源码目录。** `config.py` 从源码位置推导 `ROOT`，并在其下创建 `work`、`data`、`bin` 和凭据目录。安装到受保护目录或 macOS 应用包后，这种写入方式不能继续使用。
2. **工具发现不统一。** `media.py` 的本地 FFmpeg 路径硬编码 `.exe`；`bili_upload.py` 优先查 PATH；多个模块分别发现工具，可能在用户机器上调用错误版本。
3. **交互依赖终端。** `login()` 直接运行 biliup 并继承标准输入输出；上传会 `print` 原始输出；下载和校验以日志呈现进度。GUI 需要可订阅的结构化事件。
4. **缺少长期运行的调度入口。** 现有 `run_many()` 面向一次性批处理。桌面端需要持续接收新任务，并统一调度创建、重试、修复和确认投稿。
5. **线程锁不足以约束多个进程。** 当前上传锁位于 Python 进程内，同时启动 GUI 与 CLI 或多个实例仍可能并行投稿。
6. **现有状态需要补充桌面恢复语义。** 没有明确的取消、中断和投稿结果待确认状态。当前上传命令成功但没有 BV 号时，仍会记录 `submitted` 并保留素材，界面必须区分这种情况。
7. **开发依赖不能直接当发布清单。** Python 依赖目前使用最低版本约束；发布需固定经过验证的版本、架构和文件校验值。

### 2.3 三期都必须保持的业务约束

- 下载、校验、上传各单路，跨阶段并行；不增加“上传并发数”设置。
- 上传尝试结束后的默认间隔为 20 秒，允许按现有配置调整；界面展示等待原因和剩余时间。
- 完整 MP4 保持原画质并直接复用；非 MP4 才按现有规则转换，不为了界面体验自动降清晰度。
- 校验失败不得进入上传；批量流程保留最多五轮下载/校验的现有机制。
- 校验缓存继续校验整文件 SHA-256 和工具状态；不能以“存在文件”或“.part 有时长”判断下载成功。
- 获得 BV 号后才允许执行现有任务目录清理；没有 BV 号、上传失败、修复结果均保留文件。
- `repair` 只准备替换素材，不重新投稿，不改写原 BV 号。
- 投稿成功表示提交完成，界面不得显示为“审核通过”。
- 保留 CLI；桌面端和 CLI 复用业务层，避免出现两套下载和投稿实现。

## 3. 技术路线与整体架构

### 3.1 推荐主方案

采用 **React + TypeScript + Vite + Tauri 2 + Python 业务进程**。

- React/TypeScript：实现任务、登录、设置和素材预览界面，使用 CSS 设计变量承接官网风格。
- Tauri：提供桌面窗口、文件选择、系统目录、打开文件夹、凭据访问、单实例控制和安装包构建。
- Python：继续承担 yt-dlp、DeepL、媒体处理、任务队列与 biliup 调用。
- PyInstaller：把 Python 业务进程及其 Python 依赖冻结，作为桌面应用随附的后台程序。
- SQLite：保持本地任务存储，新增数据库迁移和操作记录。

这条路线能使用 Web 布局精确表达官网的风格，同时复用现有 Python 核心。代价是开发端需要维护前端、Rust 外壳和 Python 三部分工具链；应把 Rust 代码控制在桌面集成和通信范围内。

Tauri 官方支持将 Python 冻结程序作为 sidecar 随应用分发，并要求按目标平台架构配置外部二进制文件。实现时依照其 [sidecar 文档](https://v2.tauri.app/develop/sidecar/) 配置。

PySide6 可以减少语言种类，但本方案优先采用 Web 风格界面和跨平台安装流程；Electron 也可实现需求，但本项目不需要随应用额外分发完整 Chromium。以上是本项目的工程取舍，不作为绝对性能结论。

### 3.2 运行关系

```text
React 页面与组件
    ↓ 调用受限 Tauri command；订阅 Tauri event
Tauri / Rust 桌面主进程
    ↓ stdin/stdout JSON Lines；管理进程生命周期
Python desktop worker（唯一业务调度入口）
    ├─ TaskService / Scheduler / TaskStore
    ├─ 下载队列 → 校验队列 → 素材准备与上传队列
    ├─ yt-dlp / DeepL / Pillow
    └─ FFmpeg / ffprobe / biliup / Deno 子进程
```

不开放 HTTP 服务或固定 localhost 端口。发布版加载本地前端文件，业务通过进程管道通信。开发版可由 Tauri 启动虚拟环境中的 worker；第一期后半段同时验证冻结版 worker 的启动。

### 3.3 通信契约

新增协议版本 `protocol_version`，启动时握手并报告应用版本、业务版本、工具版本和能力。版本不兼容时阻止开始任务，并展示可诊断错误。

请求包含 `request_id`、`method`、`params`；响应包含相同请求 ID、`result` 或结构化 `error`。长任务立即返回已入队结果，不让前端等待整个视频处理完成。

拟定方法：

- `system.health`、`system.diagnostics`。
- `settings.get`、`settings.update`、`credentials.set`。
- `auth.login.start`、`auth.login.cancel`、`auth.status`、`auth.renew`。
- `tasks.create`、`tasks.list`、`tasks.get`、`tasks.retry`。
- `tasks.update_metadata`、`tasks.submit`、`tasks.cancel`、`tasks.repair`。
- `logs.tail`、`logs.export`。

事件包含 `event_id`、`worker_session_id`、时间、任务 ID、执行批次 ID、事件类型及负载。事件类型包括 `task.status`、`task.progress`、`task.log`、`auth.qrcode`、`auth.status`、`worker.error`。

工程约束：

1. stdout 只允许协议消息，普通日志写 stderr 或本地轮转日志；重构现有 `print`，避免协议被日志污染。
2. 明确 UTF-8、逐行组帧、单消息大小上限、断行缓冲、错误消息和请求超时；多线程通过单一发送队列写出事件。
3. 进度事件限频，建议每任务每秒最多四次；日志批量推送，前端列表虚拟化，避免大量输出拖慢窗口。
4. 设置、读取、取消等控制请求不能被下载线程阻塞；调度工作在后台线程，主协议循环持续读取请求。
5. 重连或 worker 重启后先读取数据库快照；事件负责即时更新，持久状态负责恢复，前端不自行推断任务完成。
6. `tasks.create` 和 `tasks.submit` 使用操作 ID 去重，重复点击或请求重试不能触发重复投稿。
7. 密钥不出现在命令行参数、事件、浏览器存储或日志；凭据写入请求只传递给受控本地处理链路。

### 3.4 目录、凭据与进程生命周期

从第一期新增 `AppPaths` 和 `ToolRegistry`，区分四类路径：

- 只读资源：前端静态文件、图标、随包工具和 Python 运行库。
- 用户数据：数据库、非敏感设置、任务恢复信息。
- 工作目录：下载视频、封面、缓存和修复文件，可由用户选择磁盘位置。
- 日志与凭据：独立目录，日志可轮转，凭据限制访问。

建议 Windows 数据根目录为 `%LOCALAPPDATA%\StarDazz\yt2bili\`，macOS 为 `~/Library/Application Support/StarDazz/yt2bili/`。由桌面层通过系统目录 API 解析实际绝对路径并传入 worker，不依赖启动时的当前目录。

DeepL 密钥通过系统凭据存储封装，Windows 使用系统凭据机制，macOS 使用 Keychain。biliup 和 yt-dlp 需要读取 Cookie 文件时，写入受限用户目录，并明确管理其有效期和清除动作；不宣称这些第三方需要的文件天然加密。

工具选择统一为“用户明确指定并通过检测的版本 → 随包固定版本 → 开发模式下的 PATH 备用”。发布版不静默调用未知 PATH 工具，也不自动下载 latest 覆盖随包工具。

桌面主进程负责 worker、worker 负责全部媒体与上传子进程。正常退出依次停止接收任务、持久化状态、结束或等待子进程、关闭数据库；异常退出后启动时恢复中断任务。Windows 使用进程组/Job Object 等机制管理子进程树，macOS 使用进程组和信号管理，不能只关闭窗口留下上传进程。

## 4. 官网视觉参考与界面设计规范

### 4.1 已核实的参考特征

2026-09-19 实际打开 [StarDazz 官网首页](https://stardazz-com.vercel.app/) 并读取页面样式。本次观察到的是深色主题：

- 背景接近 `#09090B`，整体使用黑白灰，主文字近白、辅助文字偏灰。
- 字体栈为 `Inter, ui-sans-serif, system-ui, sans-serif`。
- 品牌文字简洁，首页使用大标题、充足留白、细分隔线。
- 主按钮为浅色实底胶囊，次按钮为低对比背景和细边框胶囊。
- 产品卡片圆角约 16px，语言和主题小按钮圆角约 6px。
- 页面提供语言和明暗主题切换入口，已有 yt2bili 产品入口。

下列桌面尺寸、浅色值和状态色是根据上述特征制定的产品规范，属于建议值，并非声称全部从官网提取。

### 4.2 设计变量

- 深色主题：背景 `#09090B`，面板 `#18181B`，边框 `#27272A`，主文字 `#FAFAFA`，次文字 `#A1A1AA`。
- 浅色主题：背景 `#FAFAFA`，面板 `#FFFFFF`，边框 `#E4E4E7`，主文字 `#18181B`，次文字 `#71717A`。
- 主按钮：深色主题采用浅底深字；浅色主题采用深底浅字。红色只用于危险动作或错误提示，不把现有图标中的颜色扩展成全屏强调色。
- 状态色：完成、运行、等待、失败分别使用少量绿色、蓝色、琥珀色、红色，并始终配文字或图标。
- 字号：正文 14px，辅助文字 12px，页标题 24～28px；日志使用系统等宽字体。任务页面不照搬官网 60px 的宣传标题。
- 圆角：面板 16px、输入框 8px、主要操作按钮胶囊形；任务列表保留紧凑行布局。
- 间距：以 4px 为基本单位，页面边距 24px，区域间距 24～32px。
- 字体：打包授权允许的 Inter 字体，中文使用系统字体回退；不依赖在线字体服务。
- 动效：只做 120～180ms 的状态过渡；支持减少动画设置，避免持续闪烁。

### 4.3 页面结构

默认窗口建议 1280×800，最小 1024×680；支持系统缩放、高 DPI 和键盘操作。保留系统原生窗口控制，macOS 预留交通灯区域。

```text
┌─────────────────────────────────────────────────────────────┐
│ 系统标题栏                         yt2bili · StarDazz         │
├─────────────┬───────────────────────────────────────────────┤
│ yt2bili     │ 任务中心                    新建任务            │
│             │ 下载 1 · 校验 1 · 上传 1 · 等待 4              │
│ 任务中心    ├───────────────────────────────────────────────┤
│ 投稿记录    │ 搜索 / 状态筛选                               │
│ 账号与连接  │ 视频标题   当前阶段   进度/等待原因    操作     │
│ 设置        │ ……                                            │
│             ├───────────────────────────────────────────────┤
│ 主题 / 关于 │ 选中任务详情：素材 / 参数 / 日志                │
└─────────────┴───────────────────────────────────────────────┘
```

页面职责：

1. **任务中心**：粘贴多条链接、导入 TXT、去重、查看队列、进入详情、重试或取消。
2. **任务详情**：原始信息、译文、封面、文件信息、阶段进度、错误解释和日志；准备完成后可修改标题简介，再确认投稿。
3. **投稿记录**：历史状态、时间、BV 号、打开 B 站稿件、准备修复素材；素材已清理时展示原因。
4. **账号与连接**：B 站扫码、登录态刷新、DeepL 配置与检测、YouTube Cookie 导入或浏览器导出。
5. **设置**：工作目录、投稿分区、标签、线路、上传间隔、主题、校验模式、日志导出和工具版本诊断。

默认简体中文，支持浅色、深色和跟随系统；语言资源集中管理，完整多语言翻译不列入三期必交范围。

空列表、加载、失败、无效设置、登录过期和磁盘不足必须有明确状态。禁用按钮说明原因；长耗时操作说明当前阶段，不使用虚构进度。

## 5. 第一期：可视化操作界面

### 5.1 目标和范围

交付可由开发命令启动的真实桌面应用，完成从首次配置到投稿结果查看的全流程；先在 Windows 开发环境验收，同时保持跨平台结构。此期不交付正式安装器，但必须完成冻结 worker 的最小启动验证，提前暴露打包阻碍。

### 5.2 工作包 P1-01：桌面骨架与视觉基础

- 新增 `desktop/` 前端和 Tauri 工程，接入 React、TypeScript 和 CSS 设计变量。
- 完成导航、按钮、输入框、卡片、对话框、任务行、状态标记和日志组件。
- 实现主题持久化、系统缩放、快捷键、键盘焦点和系统标题栏适配。
- 先完成带假数据的关键页面，再替换为真实协议调用；假数据只存在于开发和测试入口。

交付：可点击的页面骨架、设计变量文件、深浅主题关键页面截图。

### 5.3 工作包 P1-02：业务服务与调度改造

- 新增桌面 worker、请求分发器、事件队列和 TaskService。
- 将 `run_many()` 内部阶段操作抽成可复用 Scheduler；一次性 CLI 批次和长期运行 GUI 调度器共用阶段实现。
- GUI 中单条、批量、重试、修复和确认投稿均进入同一个调度器，不为每个按钮另开完整 pipeline。
- 增加明确的运行 ID、阶段、错误类型、重试次数、恢复点和操作去重记录。
- 对同一工作目录加任务互斥；新增以凭据文件规范路径及可获得的账号标识为键的跨进程投稿锁，GUI/CLI 均遵守。若 CLI 使用另一份相同账号 Cookie，需识别账号后合并锁；未识别前禁止并行投稿入口。
- GUI 单实例启动；不能用单实例窗口控制替代上述 GUI/CLI 共享的投稿锁。
- 上传间隔记录迁入共享持久层，防止应用重启或另一进程启动绕过间隔。

### 5.4 工作包 P1-03：任务创建、预览和投稿

新建任务支持单条或多条 YouTube 视频链接，以及 UTF-8 TXT 文件；支持空行和 `#` 注释，按视频 ID 去重。无效链接显示具体行号；首期不扩展到整频道、播放列表抓取。

提供两种模式：

- **准备素材并预览（默认）**：下载、校验、封面、翻译后进入 `ready`，用户可查看和修改标题简介，再选择确认投稿。
- **自动投稿**：创建任务时明确选中，完成准备后按队列提交，保留现有 CLI 自动处理能力。

“确认投稿”需要新增明确的 `submit_ready()` 服务入口，重新检查文件和状态，通过统一上传队列执行；不依赖重新 `run --force`，避免重新下载、覆盖编辑结果或重复提交。

元数据编辑只允许在可编辑状态操作；保存时校验标题长度、标签、分区和简介长度，保留来源信息。设置作为任务快照保存，运行中更改全局默认值不应悄悄改变已排队任务。

封面展示使用受限本地资源访问；前端只获得授权的文件句柄或资源地址，不能任意读取用户文件。

### 5.5 工作包 P1-04：结构化进度、日志与恢复

- yt-dlp 使用进度回调获取下载字节、总量、速率和剩余时间。
- FFmpeg 校验从现有解析位置发出轨道和时间进度，转换阶段使用工具可提供的进度数据。
- biliup 输出解析封装为版本适配器；只有能可靠获得上传进度才显示百分比，否则显示“上传中”和耗时。
- 下载总量未知时显示不定进度；校验缓存哈希阶段与完整解码阶段分别标注，不伪造总百分比。
- 阶段日志写入独立日志目录，上传后清理素材不应删除全部诊断记录；导出前脱敏 Cookie、Token、API Key 和敏感参数。
- 一条任务失败不阻断其它任务；界面提供可行动的原因，例如登录失效、磁盘不足、网络失败或媒体损坏。

新增状态建议：`cancel_requested`、`cancelled`、`interrupted`、`submission_unknown`。它们是数据库状态机扩展，不是单纯前端文案。

取消和退出规则：

1. 未开始任务可以撤出队列；下载和校验通过取消令牌及子进程管理停止，保留可恢复文件。
2. 上传中优先等待本次提交结束。强制中断或连接丢失后进入 `submission_unknown`，不自动重投。
3. 命令返回成功却没有 BV 号也进入待核对状态；旧记录 `submitted` 且 BV 为空时显示相同提示。
4. 用户核对创作中心后可以登记已有 BV 号，或明确选择重新提交；确认 BV 号归属后才允许清理素材。
5. 启动时把未完成的运行中状态转为可恢复中断状态；不自动继续任何结果未知的上传。
6. 关闭窗口且有任务运行时提供等待完成或停止并退出，第一期不增加托盘常驻的第二套生命周期。

### 5.6 工作包 P1-05：可视化登录与配置

这是第一期最先验证的技术风险，不应拖到页面全部完成后才处理。

- 固定并探测 biliup 版本，在隔离登录目录中执行登录；观察二维码文件、终端交互、过期行为和 Cookie 输出位置。
- 若工具提供可读取的二维码图片或内容，转为 `auth.qrcode` 事件；若只有 TTY 菜单，增加该版本的 PTY 适配和输入状态机，不能仅把终端文字塞进页面就算完成。
- 若所选版本无法稳定自动化，应在本工作包内切换经过验证的 biliup 版本或实现独立扫码登录适配层，产出符合上传工具要求的 Cookie 文件，并单独验证。Cookie 文件导入可作为备用路径，但不能替代扫码登录的验收。
- 登录界面支持二维码加载、待扫描、待确认、成功、过期刷新、失败和取消；新旧登录会话不能交叉覆盖凭据。
- DeepL 密钥掩码显示，提供显式连接检测；没有密钥时先提示配置，不在下载大量文件后才报错。
- YouTube Cookie 优先提供文件导入；浏览器导出仍可用，但浏览器锁定、系统加密或权限失败时提供可理解的说明，不承诺任意浏览器都能直接读取。
- 首次启动向导依次完成目录、依赖诊断、DeepL 和 B 站配置；配置未完成仍可查看本地历史和设置。

### 5.7 数据迁移与兼容

- 用 SQLite schema version 管理迁移，新增运行/操作记录表，保留现有 `tasks` 的视频 ID 主键语义。
- 迁移前备份；迁移和新字段写入使用事务；失败保持旧库可恢复。
- 用户可选择现有项目目录导入数据。导入前确认旧进程已停止，使用 SQLite 备份机制，不直接复制一个仍被写入的数据库文件。
- 导入核对视频与封面路径。第一期默认复制数据库和设置，素材保留原位置并记录为外部工作目录；迁移素材是显式选项，不能静默移动大型视频。
- GUI 使用用户目录设置；CLI 无新增参数时保持原有项目用法，可增加 `--data-dir` 等方式显式接入桌面数据目录。
- 两种入口共用上传锁和规则；发布版不扫描开发者的 `.env`，现有密钥只经用户主动导入。

### 5.8 第一期验收标准

- [ ] Windows 开发环境可启动真实桌面窗口，无需在终端手动执行任务命令。
- [ ] 深浅主题、任务中心、详情、历史、账号与设置页符合第 4 节规范，1024×680 和高 DPI 下无主要操作遮挡。
- [ ] 在 GUI 完成扫码登录、密钥配置、单条与批量输入、TXT 导入和预览确认投稿。
- [ ] 四条以上测试视频能够体现跨阶段并行，下载/校验/上传每阶段始终只有一个活动任务。
- [ ] 修改后的标题简介用于实际提交，默认预览模式不会自动投稿。
- [ ] 重复点击、相同视频的不同 URL、GUI 与 CLI 同时尝试上传均不会导致并行或重复提交。
- [ ] 下载取消、校验失败、网络断开、凭据过期、磁盘不足和 worker 异常退出均有可恢复状态。
- [ ] 上传结果未知时不自动重试，获得 BV 号才清理；修复功能始终不投稿。
- [ ] 现有三组 Python 回归测试通过；新增协议、调度、迁移和取消恢复测试通过。
- [ ] 冻结 worker 可握手、访问依赖、执行一条 dry-run；不将打包风险留到第二期才发现。

第一期交付物：桌面源码、业务层改造、开发启动说明、协议说明、页面截图、测试记录。建议阶段版本 `0.2.0-alpha`。

## 6. 第二期：Windows EXE 安装与分发

### 6.1 平台目标

首批面向 Windows x64，主要验收 Windows 11；Windows 10 22H2 作为明确标注的兼容测试目标，不据此承诺长期维护。Windows ARM64 和 32 位不属于本期范围。

最终用户无需安装 Python、Rust、Node.js、FFmpeg 或手动配置 PATH。联网仍是下载、翻译、登录和投稿业务所需条件。

### 6.2 工作包 P2-01：依赖冻结与资源清单

- 固定 Python、Python 包、前端包、Rust crate、biliup、FFmpeg/ffprobe 和 Deno 版本，提交各语言锁文件与工具清单。
- 选择 Deno 作为随包 YouTube JavaScript 运行时；开发构建需要的 Node.js 与此运行时是两种用途，不能混为一项。
- 验证 yt-dlp 所需的解析器、EJS 相关组件、证书和数据文件均被收集，不以 Python 主模块能 import 作为完整验收。
- 用 PyInstaller `onedir` 作为默认 worker 打包方式，保留内部依赖布局，避免每次启动解压整个业务运行时。参考 [PyInstaller 运行模式说明](https://pyinstaller.org/en/stable/operating-mode.html)。
- Tauri sidecar 配置处理 worker 入口；其 `_internal`、动态库与数据文件通过资源配置完整复制。增加发布目录布局检查，明确启动位置，不能只打包一个入口 exe 而漏掉同目录依赖。
- 工具清单记录来源、版本、架构、SHA-256、许可证和构建选项；按实际选择的 FFmpeg、biliup、字体等分发物整理第三方声明和所需材料。
- 打包使用明确文件允许清单，排除开发者 `.env`、Cookie、数据库、工作视频、日志、二维码和临时文件。

### 6.3 工作包 P2-02：安装器与系统集成

使用 Tauri 的 NSIS 目标生成 `setup.exe`，配置产品名称、发布者、版本、图标、快捷方式、安装目录和卸载入口。Tauri 官方区分 NSIS EXE 与 WiX MSI，本期主交付选择 EXE。参考 [Windows Installer 文档](https://v2.tauri.app/distribute/windows-installer/)。

- 默认按当前用户安装；运行时文件写入用户数据目录，不向安装目录写数据库或 Cookie。
- 安装器检测 WebView2。主安装包采用 `offlineInstaller` 方案内置安装所需内容，降低首次安装依赖在线下载的风险；包体积以实际构建结果记录。
- 正确处理已有 WebView2、缺少 WebView2、安装失败与用户取消；不假设每台机器都自带运行环境。
- 将现有品牌图像加工为多尺寸 `.ico`，检查任务栏、桌面、开始菜单及安装器显示。
- 隐藏后台工具控制台窗口，但保留 stdin/stdout 管道；Python worker 不使用会破坏协议管道的无控制台配置。
- 安装或升级前检查应用与后台任务是否仍在运行，避免覆盖正在执行的程序。
- 升级保留设置、凭据、数据库和素材；默认卸载仅移除程序，清理用户数据作为单独的明确选项。
- 本期提供版本检测和手动下载安装升级；静默更新、差分更新不纳入本期。

### 6.4 工作包 P2-03：构建、签名和发布流程

在 Windows runner 上构建 Windows Python worker 和桌面程序，禁止复用开发机现有虚拟环境作为发布目录。流程固定为：

```text
干净检出 → 固定依赖安装 → 回归测试 → 前端构建
→ 工具校验与 worker 冻结 → 发布目录自检 → Tauri/NSIS 打包
→ 应用/安装器签名与时间戳验证 → 干净系统安装测试
→ 生成 SHA256SUMS、版本说明和发布附件
```

配置 Windows 代码签名凭据，签名密钥只保存在受控发布环境。验证应用、后台可执行组件和安装器的签名策略及最终签名有效性。签名不等于保证 SmartScreen 永不提示，需测试实际下载来源下的首次安装体验。

没有签名凭据时可以交付明确标注的内部测试包；不把它描述为已完成正式签名发布。发布所需凭据和负责人应在第二期开始前落实。

产物规划：

- `yt2bili-<version>-windows-x64-setup.exe`。
- `SHA256SUMS.txt`、`THIRD_PARTY_NOTICES.txt`、版本说明。
- Windows 安装、升级、卸载与业务冒烟测试报告。

### 6.5 第二期验收标准

- [ ] 在未安装 Python、Node.js、Rust、FFmpeg 的干净 Windows x64 系统完成安装、启动和依赖自检。
- [ ] 缺少 WebView2 时安装流程可完成，安装失败时提示清楚。
- [ ] 离线可以安装并打开界面、读取历史；联网后能完成登录、dry-run 和经授权的真实投稿。
- [ ] 中文用户名、带空格路径、自定义工作盘、普通用户权限和高 DPI 均通过验证。
- [ ] 没有后台命令行窗口反复弹出；退出后无孤立 worker 或 FFmpeg/biliup 进程。
- [ ] 从上一测试版覆盖升级后任务和凭据保留；迁移失败可恢复；默认卸载不删除用户视频。
- [ ] 安装包不包含真实密钥、Cookie、数据库或个人视频，哈希与签名验证通过。
- [ ] 全部第一期核心业务在已安装程序中完成回归，不仅在源码环境通过。

建议阶段版本 `0.3.0`，满足上述条件后作为 Windows 首个公开桌面版本。

## 7. 第三期：macOS APP、DMG 与分发

### 7.1 平台目标与前置条件

复用 React 页面、IPC 协议和 Python 业务服务；新增 macOS 平台适配与发布配置。

交付两套原生架构包：

- Apple Silicon：`aarch64-apple-darwin`，产物名使用 `macos-arm64`。
- Intel：`x86_64-apple-darwin`，产物名使用 `macos-x64`。

建议以 macOS 13 及以上作为初始兼容验证范围，最终最低版本须以选定 Python、Tauri、Deno、FFmpeg 和 biliup 的共同支持范围及实机结果锁定。Intel 包的持续支持也要以可用构建环境和依赖为准；若存在阻碍，应明确记录未交付项，不能拿 arm64 包代替 Intel 验收。

默认先分架构发布，不做 Universal 2，避免只合并外壳而遗漏 Python 扩展和媒体工具架构。

需准备 macOS 构建环境、两种架构的测试条件、Apple Developer 团队与 Developer ID Application 签名身份、公证凭据。

### 7.2 工作包 P3-01：平台行为与依赖适配

- 清除 `.exe`、反斜杠、源码根目录和固定 PATH 假设；统一通过 `ToolRegistry` 找到对应架构的程序。
- FFmpeg、ffprobe、biliup、Deno、Python 及其原生扩展全部匹配目标架构；不静默回退到另一架构并要求用户自行安装 Rosetta。
- `bili_upload.py` 现有 macOS 资源匹配逻辑包含架构回退，发布流程改用固定清单并严格校验架构。
- Finder 启动时通常不具有开发终端的 PATH，必须验证完全使用随包依赖。
- macOS 媒体校验以 CPU 作为可靠基线；当前 NVIDIA 解码和 `h264_nvenc` 不作为 Mac 能力，界面按检测结果隐藏不适用选项。
- VideoToolbox 加速属于可选后续优化，不作为第三期完成条件，也不改动完整 MP4 直接复用的规则。
- 适配 Keychain、文件选择、打开目录、Dock 图标、应用菜单、Cmd 快捷键、窗口关闭与 Cmd+Q 的退出行为。
- 验证浏览器 Cookie 访问可能遇到的 Keychain/权限问题，保留手动 Cookie 文件导入。
- 应用包内资源只读；工作目录、日志和凭据都放在用户目录。

### 7.3 工作包 P3-02：应用包与 DMG

Python worker 在对应 macOS 环境中冻结；PyInstaller 产物需要与平台匹配，不能把 Windows exe 放进 Mac 应用包。构建脚本检测实际架构，不根据 runner 名称盲目推断。

Tauri 生成 `.app`，再生成包含应用及 Applications 快捷入口的 DMG，用户拖入应用程序目录后运行。参考 [Tauri DMG 文档](https://v2.tauri.app/distribute/dmg/)。

- 加工 `.icns`，统一 Dock、Finder 和 DMG 安装背景的品牌视觉。
- 按 macOS bundle 规则放置 worker、Python 运行库、动态库、媒体工具和普通资源；验证动态库加载路径与可执行权限。
- 明确 PyInstaller onedir 内容到 `.app` 的映射；必要时增加封装启动器或布局转换，保留冻结程序所需的相对关系。
- 用 `file` 等工具检查所有 Mach-O 架构，用动态库检查工具排除开发机绝对路径依赖。
- 首次从只读 DMG 启动时提示先安装到 Applications；正式验收以下载、挂载、拖入、推出镜像再启动为准。

### 7.4 工作包 P3-03：签名、公证与 Gatekeeper 验证

正式外部分发按 [Tauri macOS 签名文档](https://v2.tauri.app/distribute/sign/macos/) 配置 Developer ID 签名和公证；同时参考 [Apple 公证说明](https://developer.apple.com/documentation/security/notarizing-macos-software-before-distribution)。

发布步骤：

1. 完成所有文件布局及必要修改，再处理嵌套 Python 扩展、动态库、worker、FFmpeg、biliup、Deno 等可执行代码的签名。
2. 按从内到外顺序签名，启用符合发布要求的 Hardened Runtime；为 Deno 等 JIT 运行组件实际验证最小必要 entitlement。
3. 对最终 `.app` 签名并验证，制作分发镜像；对交付容器按公证流程签名、提交并检查公证日志。
4. 为可装订的交付对象装订公证票据，验证最终 DMG 及其中应用，确保取出的应用仍是公证对应版本。
5. 在另一台干净 Mac 上通过浏览器下载，以保留下载隔离属性的真实路径测试 Gatekeeper；不能通过手动删除隔离属性作为发布验收。
6. 签名完成后不再修改 bundle 内二进制；依赖更新通过下一版本安装包分发。

公证失败、嵌套签名不完整或 Gatekeeper 拒绝启动时，本期只算产生了测试构建，不算完成正式分发。不能用“允许任何来源”或关闭系统安全机制替代修复。

### 7.5 第三期验收标准

- [ ] arm64 与 x64 各自生成可安装 DMG，所有随包二进制架构一致且依赖完整。
- [ ] 从浏览器下载、挂载、拖入 Applications、推出镜像、首次启动的完整过程通过验证。
- [ ] 最终应用及分发物签名、公证和票据检查通过，正常 Gatekeeper 环境可启动。
- [ ] Finder 启动不依赖 Homebrew、终端 PATH、系统 Python 或用户安装的 FFmpeg。
- [ ] 两种架构均验证登录、翻译、下载、CPU 校验、预览确认投稿、重试和修复。
- [ ] Keychain、中文路径、外部工作目录、Retina 显示、睡眠唤醒和网络切换可正确处理。
- [ ] 下载/校验中退出可恢复，上传结果未知不自动重投，无孤立子进程。
- [ ] 覆盖安装保留用户数据，删除应用不隐式删除用户素材和任务数据库。

交付：`yt2bili-<version>-macos-arm64.dmg`、`yt2bili-<version>-macos-x64.dmg`、SHA-256 清单、第三方声明、签名公证记录、安装说明和双架构测试报告。建议阶段版本 `0.4.0`。

## 8. 建议工程目录与代码责任

以下均为新增或改造规划，不表示当前仓库已存在这些文件：

```text
yt2bili/
  cli.py                    保留 CLI，接入公共服务与投稿锁
  config.py                 非敏感设置、路径与任务参数快照
  paths.py                  新增：AppPaths
  tools.py                  新增：ToolRegistry
  service.py                新增：任务与账号服务入口
  scheduler.py              新增：持久任务调度和取消控制
  events.py                 新增：进度、日志、状态事件
  desktop_worker.py         新增：JSON Lines 协议入口
  process_manager.py        新增：子进程树与平台差异
  db.py                     改造：迁移、事务、操作去重与恢复
  pipeline.py               改造：阶段函数与服务共用
  youtube.py / media.py      改造：工具路径、回调和取消
  bili_upload.py            改造：扫码、流式进度及结果核对
desktop/
  src/
    pages/ components/      页面与组件
    bridge/                 IPC 类型、调用、订阅和重连
    styles/tokens.css       StarDazz 风格设计变量
  src-tauri/
    src/                    桌面集成、凭据与 worker 管理
    capabilities/           最小权限配置
    icons/                  平台图标
    tauri.conf.json         通用应用配置
    tauri.windows.conf.json Windows 安装配置
    tauri.macos.conf.json   macOS 分发配置
packaging/
  worker.spec               PyInstaller 入口和资源规则
  tools.lock.json           固定版本、架构、来源与哈希
  windows/ macos/           平台构建和验证脚本
scripts/
  build_worker.py            冻结、收集、架构检查
  verify_bundle.py           检查布局和禁止打包的文件
tests/
  ...                       保留原有测试
  test_desktop_protocol.py   协议与错误处理
  test_scheduler_recovery.py 取消、恢复与未知提交结果
  test_db_migrations.py      旧库迁移与回滚
.github/workflows/
  desktop-ci.yml             业务、前端和外壳检查
  release-windows.yml        Windows 构建与签名
  release-macos.yml          Mac 双架构构建、签名、公证
三期桌面端实现方案.md
```

第一期先确定 worker 协议和 AppPaths，再实现页面接入。第二期不能把安装路径改动散落到各业务模块；第三期平台判断应收敛在路径、工具和进程管理模块。

## 9. 验证策略与发布门槛

### 9.1 自动化验证

- 保留并执行当前测试：`python -m unittest discover -s tests`。
- Python 新增测试聚焦队列互斥、取消、上传结果未知、状态迁移、事务和数据恢复，不仅验证界面能调到函数。
- IPC 测试覆盖分段消息、无效 JSON、未知方法、重复请求、worker 崩溃和事件积压。
- 前端执行类型检查、关键交互测试和构建；验证按钮状态、表单校验、主题和错误提示。
- 原生自动化能力按平台验证，不假设浏览器测试等于 Tauri 安装版端到端测试；macOS 原生交互保留人工验收。
- 发布检查工具架构、依赖文件、敏感文件排除、第三方声明、哈希和签名。
- CI 默认使用模拟服务和本地测试素材，不自动向真实 B 站账号投稿；发布前用已授权素材做少量真实冒烟验证。

### 9.2 通用异常矩阵

必须覆盖：无登录态、Cookie 过期、DeepL 配额或网络失败、YouTube 风控、磁盘满、目录不可写、外接磁盘断开、媒体损坏、上传失败、成功但无 BV、提交中断、应用崩溃、数据库迁移失败、依赖缺失或架构错误。

同时验证现有边界：含 `-` 的视频 ID、相同 ID 的不同链接、`.part` 续传、校验缓存失效、五轮校验重试、成功后清理失败不误报投稿失败。

### 9.3 可用性与性能目标

以下是待实测的验收目标，不是已实现指标：

- 在记录配置的参考机器上，冷启动后 5 秒内呈现可操作主界面；依赖检查可继续异步执行。
- 下载、解码、计算哈希期间窗口拖动、取消和导航保持响应，不在 UI 线程执行媒体任务。
- 1,000 条本地任务使用分页或虚拟列表；前端日志只保留有界窗口，完整日志按文件保留。
- 连续批量处理至少 10 个小型授权测试视频，无队列死锁、跨任务进度混淆和重复提交。
- 不预设安装包体积为“小于某值”；构建后分别记录前端外壳、Python、FFmpeg、Deno 和 WebView2 占用，作为后续优化基线。

## 10. 三期排期与依赖关系

按一名熟悉 Python 与前端的开发者负责实现、设计与测试人员配合估算；以下为工作日估算，不含证书申请、平台审批等待和大范围需求变更。

### 第一期：约 15～21 个工作日

1. 1～2 日：biliup 可视化登录、worker 协议、冻结启动与路径机制技术验证。
2. 3～4 日：桌面骨架、设计变量及关键页面。
3. 5～7 日：服务层、长期调度、进度、预览与投稿、凭据及迁移。
4. 3～4 日：取消、恢复、跨进程锁、异常处理。
5. 3～4 日：联调与验收。

上述子项存在交叉工作，阶段总量按整合后的 15～21 日估算；若登录工具必须新增独立适配，另预留 3～5 日。

### 第二期：约 6～9 个工作日

1. 2～3 日：固定依赖、worker 冻结和 Windows 包布局。
2. 2～3 日：NSIS、WebView2、签名与构建流程。
3. 2～3 日：干净系统安装、升级、卸载与真实业务验证。

### 第三期：约 7～11 个工作日

1. 2～3 日：macOS 路径、进程、凭据和 CPU 媒体适配。
2. 2～3 日：双架构依赖、应用包和 DMG。
3. 1～2 日：嵌套签名、公证和 Gatekeeper 问题修复。
4. 2～3 日：双架构实机回归和发布验收。

总估算约 **28～41 个工作日**，复杂扫码适配和签名环境首次搭建可能增加时间。第一期的调度、路径和协议完成是第二期的前置条件；第三期沿用第二期固定依赖和构建规则，不能直接复用 Windows 二进制。

## 11. 风险处理与启动前决策

### 11.1 主要风险与处理

- **biliup 登录只支持终端交互**：第一期最前面验证二维码和输入协议，固定版本并添加集成测试；扫码不能以“稍后接入”带入第一期验收。
- **外部平台或解析规则变化**：固定发布版本，保留依赖版本诊断，安排依赖升级回归；不让已签名应用自行替换包内工具。
- **任务积压导致磁盘耗尽**：显示工作目录可用空间，低空间时停止安排新下载，已有上传和清理可继续；未知视频大小下仍需持续检查。
- **投稿结果丢失引起重复发布**：持久操作 ID、跨进程锁、待核对状态和人工登记 BV；不以网络重试实现盲目重新投稿。
- **Windows 安装权限、运行时或签名问题**：第二期使用干净机器验证标准用户路径、WebView2 和实际下载体验。
- **macOS 嵌套依赖签名失败**：提前检查每个原生组件、JIT entitlement 和动态库路径；公证日志作为发布检查依据。
- **双架构第三方工具缺失**：构建前核对清单；缺包时选择可复现的源码构建或兼容版本，未解决则明确该架构尚未通过验收。
- **数据库升级破坏旧数据**：备份、版本化迁移、失败回滚；不保证新版数据库能被旧版程序直接读取，降级使用对应备份。

### 11.2 建议默认决策

本方案默认：产品名 `yt2bili`，品牌署名 StarDazz；默认中文、跟随系统主题；新任务默认先预览；Windows 先做 x64；macOS 分 arm64/x64 两包；三期均本地运行；手动下载安装更新。

进入对应阶段前需要落实的外部资源：

- 发布者显示名称、稳定应用标识（拟用 `com.stardazz.yt2bili`，实施前确认）、图标源文件和字体资源。
- Windows 签名方式与凭据保管人；Apple Developer 团队、签名身份及公证凭据。
- Windows 干净测试环境、Apple Silicon 与 Intel 的构建和测试条件。
- 用于真实登录和投稿验收的测试账号、授权视频以及不重复发布的测试规则。
- 最终下载渠道；默认先上传项目 Release，再由官网链接到对应平台安装包。官网改版不是本三期的必要工程范围。

### 11.3 暂不纳入三期范围

多账号同时投稿、云端同步、团队协作、远程控制、手机端、字幕生成、视频剪辑、整频道抓取、自动审核结果监控、静默自动更新和应用商店上架。后续扩展以三期桌面基础稳定为前提。

## 12. 总体验收定义

项目完成三期的标志是：用户从 Windows EXE 安装器或对应 Mac 架构 DMG 安装应用后，无需配置开发环境，即可通过符合 StarDazz 视觉风格的界面完成配置、登录、视频任务处理、预览和投稿；出现错误时能理解状态并恢复，升级后保留数据，正式发布包通过平台签名及相应分发验证。

本方案中的每个阶段须独立提供可运行交付物和验收记录。界面截图不能替代业务闭环，构建命令成功不能替代干净系统安装验证，生成 DMG 不能替代签名、公证和真实下载启动验证。
